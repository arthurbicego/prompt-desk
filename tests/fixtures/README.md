# PromptDesk technical fixtures

These fixtures model Codex Home and project directory shapes used by PromptDesk scanner and safety tests.

The files intentionally use placeholder values only. Secret-looking fixtures are present to verify safety blocking and must not contain real credentials.
